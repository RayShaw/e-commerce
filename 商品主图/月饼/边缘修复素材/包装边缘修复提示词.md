# 包装边缘修复提示词

用户授权以主图1为模板补全外围包装。输入1为对应的旋转提亮版，输入2为主图1，仅参考包装边缘。内置 image_gen 结果为边缘合成素材，不能直接代替正式主图；须运行上级目录的月饼边缘合成.py，覆盖回实拍压印和所有标签文字。

```text
Use case: precise-object-edit. Input image 1 is the edit target. Input image 2 is ONLY a reference for the outer plastic packaging edge finishing style. Output exactly 1254x1254. Repair ONLY the outer perimeter plastic wrapper of image 1: complete clipped gold crimp seals at top and bottom, join disconnected transparent film, clean cutout artifacts, make four outer edges look like a complete real commercial plastic wrapper with modest natural folds and realistic soft shadows on pure white. Take material/edge finish cues from image 2, NOT its mooncake. CRITICAL preserve exact image 1 composition, scale, location and rotation, all cake pixels, embossing and every label, emblem and curved small printed text. Do not move, resize, replace or rotate the product, tray, cake, or text. Preserve the central tray completely unchanged; only extend/repair wrapper outside tray perimeter, keep comfortable white margin and no cropped edges. Match existing image 1 lighting and color. No new text, no retyping, no design changes, no extra objects. This is a perimeter-only restoration, not a new product render.
```
